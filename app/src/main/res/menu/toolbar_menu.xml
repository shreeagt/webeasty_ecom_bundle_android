<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item android:id="@+id/item_search"
        android:title="search"
        android:icon="@drawable/ic_bx_search"
        app:showAsAction="always"/>

    <item android:id="@+id/item_favorite"
        android:title="Favorite"
        android:icon="@drawable/ic_round_favorite_border"
        app:showAsAction="always"/>

    <item android:id="@+id/item_bag"
        android:title="Bag"
        android:icon="@drawable/ic_bx_shopping_bag_outline"
        app:showAsAction="always"/>


</menu>